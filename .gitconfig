[alias]
	st = status
	ch = checkout
	br = branch
	c = commit
	tags = tag --sort=taggerdate
	tree = log --pretty=format:'%C(yellow)commit %H%Creset%C(green)%d%Creset%nCommitter: %cn <%ce>%n%n    %s%n    %cr%n' --graph
	last = log -1
	refresh = fetch -p
[core]
	editor = /usr/bin/vim

